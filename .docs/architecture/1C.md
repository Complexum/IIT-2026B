# Plan 1C — Estado Real Post-Implementación TUI

## Cambios Arquitectónicos Realizados

### Movimientos & Eliminaciones

| Acción | Archivo | Razón |
|--------|---------|-------|
| **CREADO** | `src/io/manager.py` | Capa IO: `listar_redes`, `cargar_mpt`, `crear_sistema`, `eliminar_red`, `listar_resultados`, `cargar_resultados` |
| **CREADO** | `src/io/generator.py` | `generar_red` + `peso_estimado` (estimación de tamaño) |
| **ELIMINADO** | `src/infra/app.py` | Contenía `generar_red` roto |
| **ELIMINADO** | `src/iit/strategies/python/phi/consts.py` | Duplicado de `src/iit/strategies/consts.py` |
| **RENOMBRADO** | `src/tui/config/` → `src/tui/results/` | La 4ta tab es Results |

### TUI — 4 Tabs Funcionales

| Tab | Archivos | Estado |
|-----|----------|--------|
| **Dataset** | `dataset/{screen,widgets,helpers,styles}.py` | ✅ Funcional — red list, operaciones real-time, bento layout |
| **Testing** | `test/{screen,widgets,helpers,generadores,styles}.py` | ✅ Funcional — patrón CRUD, generadores, preview |
| **Execution** | `run/{screen,widgets,helpers,styles}.py` | ✅ Funcional — program CRUD, selectors, progress bar |
| **Results** | `results/{screen,widgets,styles}.py` | ✅ Funcional — result list, CSV parsing, detail view |

### Nomenclatura

Todas las funciones son en español, clases en inglés, carpetas en inglés.
Métodos privados usan doble guión bajo (`__`).
Imports siempre con prefijo `src.`.

---

## Árbol Actual

```
src/
├── iit/                         # Dominio IIT
│   ├── base/                    # Configuración y constantes
│   │   ├── enums/               # MetricDistance, TimeEMD, Notation
│   │   ├── app.py               # Application singleton
│   │   ├── consts.py            # Constantes IIT
│   │   └── platform.py          # Detección HW
│   ├── core/                    # Estructuras de datos fundamentales
│   │   ├── ncube.py             # NCube dataclass ✅
│   │   ├── system.py            # System dataclass ✅
│   │   ├── solution.py          # Solution ✅
│   │   └── params.py            # (futuro)
│   └── strategies/              # Algoritmos de resolución
│       ├── consts.py            # Constantes compartidas
│       ├── python/
│       │   ├── sia.py           # SIA base class ⚠️
│       │   ├── basic/code.py    # Estrategia básica ⚠️
│       │   ├── force/code.py    # BruteForce (vacío)
│       │   ├── qnodes/          # Q-Nodes ⚠️
│       │   └── phi/             # Pyphi wrapper ⚠️
│       └── rust/                # Wrappers PyO3 (futuro)
│
├── infra/                       # Infraestructura cross-cutting
│   └── middlewares/
│       ├── color.py             # ColorFormatter ✅
│       ├── profile.py           # ProfilingManager ✅
│       └── slogger.py           # SafeLogger ✅
│
├── io/                          # Capa de datos
│   ├── manager.py               # CRUD redes + resultados ✅
│   └── generator.py             # Generación redes TPM ✅
│
├── tui/                         # Interfaz Textual
│   ├── app.py                   # MIPApp — 4 tabs ✅
│   ├── styles.scss              # CSS global ✅
│   ├── shared/
│   │   ├── base.py              # IITHeader, IITFooter ✅
│   │   └── consts.py            # estilizar() — carga CSS adyacente ✅
│   ├── dataset/                 # Tab 1: Dataset
│   │   ├── screen.py            # DatasetScreen ✅
│   │   ├── widgets.py           # NetworkItem ✅
│   │   ├── helpers.py           # parsear_estado, parsear_indices, formatear_sistema ✅
│   │   └── styles.scss          # CSS bento ✅
│   ├── test/                    # Tab 2: Testing
│   │   ├── screen.py            # TestingScreen (editar + preview) ✅
│   │   ├── widgets.py           # PatternItem, GeneratorItem ✅
│   │   ├── helpers.py           # Patron CRUD, combinaciones ✅
│   │   ├── generadores.py       # Registro de generadores binarios ✅
│   │   └── styles.scss          # CSS cuadrantes ✅
│   ├── run/                     # Tab 3: Execution
│   │   ├── screen.py            # ExecutionScreen ✅
│   │   ├── widgets.py           # ProgramCard ✅
│   │   ├── helpers.py           # Programa CRUD, listar_estrategias ✅
│   │   └── styles.scss          # CSS cards ✅
│   └── results/                 # Tab 4: Results
│       ├── screen.py            # ResultsScreen ✅
│       ├── widgets.py           # ResultItem ✅
│       └── styles.scss          # CSS grid ✅
│
└── cli.py                       # Entry point CLI ✅

data/
├── input/
│   ├── networks/                # TPMs en CSV (N2A, N3A, etc.)
│   └── patrones/                # Patrones de prueba en JSON
├── output/                      # Resultados CSV
└── programas/                   # Programas de ejecución en JSON
```

---

## Estado de Verificación

```
✅ python main.py                    → Carga N3A, System, substraer, distribución marginal
✅ Todos los imports                 → IO, TUI, generadores, patrones, programas
✅ TUI headless test                 → 4 tabs montan correctamente
✅ Keyboard shortcuts (d/t/e/r/q)    → Navegación entre tabs
✅ Dataset: redes listadas           → NetworkItem con CRUD
✅ Testing: patrón CRUD              → PatternItem + GeneratorItem + cuadrantes + preview
✅ Execution: programa CRUD          → ProgramCard con Select + ProgressBar
✅ Results: resultados listados      → ResultItem + detalle formateado
✅ Generadores (8 built-in)          → todos, ninguno, pares, impares, inicio, final, mult_3, completo
✅ Estrategias descubiertas          → basic, force, phi, qnodes
✅ Estimación de tiempo              → 10^(1 + (n-10)/3) — n=25 ≈ 11d
```

---

## Fases Pendientes

### Fase 2: Strategies Cleanup

| # | Servicio | Archivo | Qué hacer |
|---|----------|---------|-----------|
| **2.1** | SIA base | `strategies/python/sia.py` | Fix imports viejos |
| **2.2** | EMD + utils | `strategies/python/qnodes/func.py` | Implementar `emd_efecto` |
| **2.3** | Fix QNodes | `strategies/python/qnodes/code.py` | Migrar imports |
| **2.4** | Fix Phi | `strategies/python/phi/code.py` | Migrar imports |
| **2.5** | Fix Basic | `strategies/python/basic/code.py` | Migrar imports |

### Fase 3: Ejecución Real

| # | Qué | Archivo |
|---|-----|---------|
| **3.1** | Conectar `ExecutionScreen.empezar` con estrategia real | `run/screen.py` |
| **3.2** | Worker async para ejecución en background | `run/helpers.py` |
| **3.3** | Guardar resultados a CSV | `io/manager.py` |

### Fase 4: Mejoras UX

| # | Qué |
|---|-----|
| **4.1** | Persistencia de config Application a JSON |
| **4.2** | Gráficas matplotlib comparativas (Results tab) |
| **4.3** | Progress bar realista con estimación exponencial |
| **4.4** | Validación de inputs (dims, estado) con feedback visual |
